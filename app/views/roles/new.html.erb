<div class="jumbotron">
	<h1>Add new Role</h1>
	<%= form_for @role do |f| %>
		<div class="form-group">
	    <label for="exampleInputEmail1">Name</label>
	 		<%= f.text_field :name %>
	 		</div>
	 		<div class='form-group'>
				<%= f.submit class: 'btn btn-primary'%>
			</div>
	<% end %>
</div>
<%= link_to "Hide Roles" , roles_path, class: 'btn btn-primary'%>